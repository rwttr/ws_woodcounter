# Wood Counter — run configuration
# Pass to predict.py with: python predict.py --config config.yaml

# Path to trained YOLOv8 weights (copied from runs/detect/.../weights/best.pt)
model: best.pt

# Directory containing the input image. Drop exactly one image file here.
image_dir: images

# Directory where count.txt and the annotated result image are written
output_dir: results

# Inference device: 'cpu', 'cuda:0', 'mps'
device: cpu

# Detection confidence threshold (0.0–1.0)
confidence: 0.5
